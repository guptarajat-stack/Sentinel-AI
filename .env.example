# ── Backend ─────────────────────────────────────────────────────────────────
PORT=5000
NODE_ENV=development
DATABASE_URL="postgresql://postgres:password123@localhost:5432/ai_security_soc?schema=public"
JWT_SECRET="super-secret-jwt-token"

# ── Frontend ─────────────────────────────────────────────────────────────────
NEXT_PUBLIC_API_URL="http://localhost:5000"

# ── AI Agents — LLM ──────────────────────────────────────────────────────────
OPENAI_API_KEY="your-openai-api-key"
OPENAI_MODEL="gpt-4o-mini"          # or gpt-4o, or any OpenAI chat model
ANTHROPIC_API_KEY="your-anthropic-api-key"
BACKEND_API_URL="http://localhost:5000"

# ── AI Agents — Threat Intel Enrichment ──────────────────────────────────────
# AbuseIPDB: free tier = 1 000 checks/day  https://www.abuseipdb.com/account/api
ABUSEIPDB_API_KEY="your-abuseipdb-api-key"

# VirusTotal: free tier = 4 req/min        https://www.virustotal.com/gui/my-apikey
VIRUSTOTAL_API_KEY="your-virustotal-api-key"
